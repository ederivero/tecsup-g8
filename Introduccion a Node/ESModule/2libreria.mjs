// La extension de estos archivos indican que han sido creados para trabajar con ESModule
// En lo archivos con ESModule el 'use strict' ya viene de manera predeterminada

export function sumar(num1, num2) {
  return num1 + num2;
}

export function restar(num1, num2) {
  return num1 - num2;
}
